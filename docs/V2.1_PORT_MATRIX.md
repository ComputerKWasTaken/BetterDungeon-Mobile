# BetterDungeon Mobile V2.1 Port Matrix

This ledger tracks the Android port of desktop BetterDungeon V2.1. It compares
desktop commit `da43bd51dbd961f166ac29f8f25f45a13ac4ec4e` against the desktop
`2.0.2` tag, with Mobile commit
`1d014a41473679e96f000118da333908700d9f92` as the starting point.

The desktop implementation is the behavioral source of truth. A file marked
**Adapt** must preserve the Android WebView bridge, touch UI, native services,
or another intentional Mobile difference instead of being copied wholesale.

## Status legend

- **Complete**: V2.1 behavior is already present or was reconciled in Stage 1.
- **Partial**: part of the desktop behavior exists, but later work is required.
- **Pending**: the V2.1 behavior is not yet available on Mobile.
- **Excluded**: intentionally desktop-only or not shipped by the Mobile app.
- **Release**: documentation or packaging work reserved for Stage 6.

## Production runtime assets

| Desktop V2.1 asset | Mobile counterpart/disposition | Status | Stage |
| --- | --- | --- | --- |
| `background.js` | Adapt privileged extension work into the WebView polyfill and Android bridge; heartbeat/query portions exist, streaming does not | Partial | 2 |
| `core/feature-manager.js` | Preserve omission of Hotkeys and Story Card Modal Dock; Navigator is registered and defaults on | Complete | 1 |
| `core/theme-variables.css` | Add the Navigator icon token while retaining Mobile tokens | Complete | 1 |
| `features/character_preset_feature.js` | Provider-neutral Character Prefill changes are already Mobile-adapted | Complete | 1 regression |
| `features/hotkey_feature.js` | Desktop keyboard-only feature | Excluded | — |
| `features/input_history_feature.js` | Guide-mode compatibility is merged into the touch history implementation | Complete | 1 regression |
| `features/input_mode_color_feature.js` | Desktop V2.1 Guide-mode changes match Mobile | Complete | 1 regression |
| `features/navigator_feature.js` | Current Mobile file is a nonfunctional Settings-tab prototype and will be replaced by the full-screen sheet | Partial | 4 |
| `main.js` | WebFetch consent cleanup is already merged; retain WebView reinjection guards | Complete | 1 regression |
| `manifest.json` | Map new script order/resources to `InjectionEngine`; versioning is Android-owned | Pending | 3, 6 |
| `modules/ai/executor.js` | Provider router/query exists; chat, tools, continuation, deltas, and cancellation are missing | Partial | 2 |
| `modules/ai/gemini-backend.js` | Query backend exists; streaming Port adapter is missing | Partial | 2 |
| `modules/ai/openai-backend.js` | Query backend exists; streaming Port adapter and final UX refinements are missing | Partial | 2 |
| `modules/ai/module.js` | Keep public `status`/`query` ops and align the module version to `1.0.0` | Complete | 1 |
| `modules/audio/module.js` | Byte-identical to desktop V2.1 | Complete | 1 regression |
| `modules/sdk/module.js` | Preserve Mobile feature/module list, add Navigator default, and align background snapshot shape | Complete | 1 |
| `modules/webfetch/consent.js` | Removed on both platforms | Complete | 1 regression |
| `modules/webfetch/module.js` | Byte-identical protocol module backed by Android `WebFetchClient` | Complete | 1 regression |
| `popup.css` | Mobile popup lacks final provider cards, Navigator settings, and V2.1 release styling | Partial | 2, 4, 6 |
| `popup.html` | Mobile popup lacks AI Agents/Navigator controls and V2.1 release content | Partial | 4, 6 |
| `popup.js` | Provider-neutral setup exists; add Navigator default now and port final provider/read-only behavior later | Partial | 1, 2, 4 |
| `services/ai-dungeon-service.js` | Guide input mode support is already merged | Complete | 1 regression |
| `services/custom-dynamic-router.js` | Guide compatibility is merged; preserve Mobile data-URI icon support | Complete | 1 regression |
| `services/graphql-service.js` | Navigator context, Story Card reads, and verified write operations are absent | Pending | 3, 5 |
| `services/navigator/context.js` | Add bounded adventure grounding and fallback warnings | Pending | 3 |
| `services/navigator/mutations.js` | Add approval-only proposal and conflict-safe apply layer | Pending | 5 |
| `services/navigator/primer.js` | Add desktop primer version 3 | Pending | 3 |
| `services/navigator/session.js` | Add per-adventure streaming sessions, persistence, tool loop, and abort handling | Pending | 3 |
| `services/navigator/tools.js` | Add bounded Story Card search and read tools | Pending | 3 |
| `services/ultrascripts/core.js` | Source-equivalent V2.1 liveness heartbeat (line endings differ only) | Complete | 1 regression |
| `services/ultrascripts/ws-interceptor.js` | Behavior matches desktop; remaining diff is formatting only | Complete | 1 regression |
| `styles.css` | Current styles contain the prototype Navigator; functional sheet/proposal styles remain pending | Partial | 4, 5 |

## Android and Mobile-only assets to preserve

| Mobile subsystem | Required treatment |
| --- | --- |
| `utils/webview-polyfill.js` | Remains the Android equivalent of the desktop background/runtime APIs; Stage 1 aligns SDK snapshots and Stage 2 adds AI streaming Ports |
| `BetterDungeonBridge.kt` and native clients | Preserve storage, popup messaging, TTS, assets, and WebFetch; extend narrowly for AI transport and Back handling only |
| `features/mobile/` | Preserve settings gear, login warning, scrollable mode menu, and other touch adaptations; remove only the superseded Navigator prototype integration |
| Android caret fix and `BetterDungeonWebView` | Preserve unchanged and verify Navigator composer behavior with the IME |
| Mobile TTS and WebFetch | Preserve native implementations and existing security limits |

## Non-runtime desktop changes

| Desktop area | Mobile disposition | Stage |
| --- | --- | --- |
| `README.md` | Rewrite Mobile release summary and version for 2.1.0 | 6 |
| `marketing/store-assets.html` | Primary desktop/store asset only | Excluded |
| `examples/aid-scripts/*` | Canonical examples remain in the primary desktop repository | Excluded |
| `tests/README.md` and `tests/aid-scripts/*` | Use as the behavioral device-test reference; do not duplicate the suites into Mobile | 2–6 regression |

## Stage gates

1. Shared defaults/contracts and this ledger are reconciled without changing later-stage behavior.
2. Gemini and OpenAI-compatible query, streaming, cancellation, and provider setup work on-device.
3. Navigator context, research tools, and persisted read-only conversation work on-device.
4. The prototype is replaced by the full-screen sheet, draggable launcher, settings, and Android Back behavior.
5. Every mutation remains proposal-only until direct approval, with conflict checks and server read-back.
6. All rows are closed, regressions pass, metadata becomes 2.1.0, and one signed release artifact is prepared.
